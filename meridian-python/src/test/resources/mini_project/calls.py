"""Usage / entry for stats_kit — Meridian --calls evidence.

Only rolling_sum and tag are exercised. unused_* must not appear in the
compiled module. tag has int + str sites so specialize+rewrite apply.
"""

rolling_sum(50)
tag(3)
tag("ab")
